#!/usr/bin/python3





"""class Task:
    task: str"""
    
tasks = []
task_id = len(tasks)
todo = "Read your book"

def Task(tasks: str):
    tasks.append(todo)
    return tasks

"""task_id = len(tasks) + 1
new_task = Task.appende("Read your book")
#new_task = task.tasks #append("read your book")
#return {"task_id": task_id} #"task": task.task }"""
print(tasks)
print(task_id)





























